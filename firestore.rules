rules_version = '2';
service cloud.firestore {
	match /databases/{database}/documents {
		match /projects/{document=**} {
			allow read;
			allow write: if request.auth.uid == 'm2aLSKU1KRdUl5XAUxVkQCuC1xA3';
		}
  }
}
